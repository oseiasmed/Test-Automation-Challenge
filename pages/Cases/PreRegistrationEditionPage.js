import { waitAndClick } from '../../lib/helpers'
import { linkCases, linkPreRegistration } from '../Cases/elementsMap'

export default class PreRegistrationEditionPage {

	async preRegistrationEd() {

		//await waitAndClick(page, linkCases)
		//await waitAndClick(page, linkPreRegistration)
		// await waitAndClick(page, preRegistrationCheckbox)
		// await waitAndClick(page, buttonEditSelected)

	}
}
